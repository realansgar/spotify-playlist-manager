const availableSources = [];

const availableFilters = [];

const state = {
  availableSources,
  availableFilters,
  groups: []
};

const getters = {};

const mutations = {};

const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
